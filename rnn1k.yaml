## Where the samples will be written
save_data: rnn/1k/samples
## Where the vocab(s) will be written
src_vocab: rnn/1k/vocab.src
tgt_vocab: rnn/1k/vocab.tgt

# Corpus opts:
data:
  corpus_1:
      path_src: src-train1k.txt
      path_tgt: tgt-train1k.txt
  valid:
      path_src: src-val1k.txt
      path_tgt: tgt-val1k.txt

# Where to save the checkpoints
save_model: rnn/1k/model 
save_checkpoint_steps: 500
train_steps: 10000
valid_steps: 500
